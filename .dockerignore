# Keep the image build context lean (the build/*.Dockerfile images build
# from the repo root). Only the Go sources matter to the image.
.git
bin/
web/node_modules/
web/.svelte-kit/
web/build/
.env
*.log
