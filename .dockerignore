# Keep the image build context lean (the build/*.Dockerfile images build
# from the repo root). Frontend assets are generated inside the build.
.git
bin/
web/node_modules/
web/.svelte-kit/
web/build/
.env
*.log

internal/webui/dist/
dist/
.cache/
