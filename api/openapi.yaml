openapi: 3.1.0
info:
  title: skali API
  description: |
    Client-facing API of skali. Authentication is email+password with
    optional TOTP two-factor; sessions are opaque bearer tokens. The same
    API serves every client: the web BFF, the `skali` CLI, and future
    native apps.

    There is no registration endpoint: users are created by an instance
    admin (`/v1/users`) or by the server operator (`skalid user create`).
    Every user carries an instance-wide role — `admin` (additionally
    manages users and instance settings) or `member` — checked per
    request. All error responses share the `Error` envelope; clients
    should branch on `error.code`, not on messages.

    Security-critical operations sit behind sudo mode: they require the
    session to have proven the user's identity recently (within the
    operator-configured reauth window). A stale session receives
    `403 reauth_required`; clients recover by confirming identity at
    `/v1/auth/reauth` and retrying the original request.
  version: 0.1.0
  license:
    name: Apache 2.0
    identifier: Apache-2.0
servers:
  - url: /
    description: Same host the spec is served from.

security:
  - bearerAuth: []

paths:
  /v1/auth/login:
    post:
      operationId: login
      summary: Log in with email and password
      description: |
        Returns a `session` when the user has no second factor, or a
        `challenge` when 2FA is enabled — exchange it with a code at
        `/v1/auth/2fa/verify` within its expiry. Exactly one of the two
        properties is present.
      security: []
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                password:
                  type: string
      responses:
        "200":
          description: Password accepted.
          content:
            application/json:
              schema:
                oneOf:
                  - $ref: "#/components/schemas/SessionCreatedResponse"
                  - $ref: "#/components/schemas/ChallengeResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "429": { $ref: "#/components/responses/RateLimited" }

  /v1/auth/2fa/verify:
    post:
      operationId: verifyTwoFactor
      summary: Complete a 2FA login
      description: |
        Exchanges a login challenge plus a 6-digit TOTP code — or a backup
        code — for a session. Challenges are single-use, expire after a few
        minutes, and are deleted after 5 failed codes.
      security: []
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [challenge_token, code]
              properties:
                challenge_token:
                  type: string
                code:
                  type: string
                  description: 6-digit TOTP code or a backup code.
      responses:
        "200":
          description: Second factor accepted; session created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/SessionCreatedResponse"
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "429": { $ref: "#/components/responses/RateLimited" }

  /v1/auth/logout:
    post:
      operationId: logout
      summary: Revoke the current session
      tags: [auth]
      responses:
        "204":
          description: Session revoked.
        "401": { $ref: "#/components/responses/Unauthorized" }

  /v1/auth/session:
    get:
      operationId: getCurrentSession
      summary: Get the authenticated user and session
      tags: [auth]
      responses:
        "200":
          description: The caller's user and session.
          content:
            application/json:
              schema:
                type: object
                required: [user, session]
                properties:
                  user: { $ref: "#/components/schemas/User" }
                  session: { $ref: "#/components/schemas/Session" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /v1/auth/sessions:
    get:
      operationId: listSessions
      summary: List active sessions
      tags: [auth]
      responses:
        "200":
          description: All unexpired sessions of the caller, newest first.
          content:
            application/json:
              schema:
                type: object
                required: [sessions]
                properties:
                  sessions:
                    type: array
                    items: { $ref: "#/components/schemas/Session" }
        "401": { $ref: "#/components/responses/Unauthorized" }

  /v1/auth/sessions/{id}:
    delete:
      operationId: revokeSession
      summary: Revoke one of the caller's sessions
      tags: [auth]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Session revoked.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "404": { $ref: "#/components/responses/NotFound" }

  /v1/auth/reauth:
    post:
      operationId: reauthenticate
      summary: Confirm identity for sudo mode
      description: |
        Re-proves the caller's identity and refreshes the current session's
        sudo-mode window; other sessions of the same user are unaffected.
        Factor policy: accounts with confirmed two-factor present a TOTP or
        backup code (`code`; the code is consumed), all others present their
        `password`.
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                password:
                  type: string
                code:
                  type: string
      responses:
        "204":
          description: Identity confirmed; the session is fresh again.
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "429": { $ref: "#/components/responses/RateLimited" }

  /v1/auth/password:
    post:
      operationId: changePassword
      summary: Change the password
      description: |
        Revokes every other session; only the requesting one survives.
        Sudo-mode gated; additionally requires the current password.
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [current_password, new_password]
              properties:
                current_password:
                  type: string
                new_password:
                  type: string
                  minLength: 8
      responses:
        "204":
          description: Password changed.
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/ReauthRequired" }

  /v1/auth/2fa/enable:
    post:
      operationId: enableTwoFactor
      summary: Start TOTP enrollment
      description: |
        Returns the TOTP secret, its otpauth:// URI (QR-code source), and the
        backup codes — none of which are ever retrievable again. Enrollment
        stays pending, and login stays password-only, until the code is
        confirmed at `/v1/auth/2fa/confirm`. Calling this again while pending
        replaces the enrollment. Sudo-mode gated; no request body.
      tags: [auth]
      responses:
        "200":
          description: Pending enrollment created.
          content:
            application/json:
              schema:
                $ref: "#/components/schemas/TwoFactorEnrollment"
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/ReauthRequired" }
        "409": { $ref: "#/components/responses/Conflict" }

  /v1/auth/2fa/confirm:
    post:
      operationId: confirmTwoFactor
      summary: Activate a pending TOTP enrollment
      tags: [auth]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [code]
              properties:
                code:
                  type: string
                  description: 6-digit TOTP code from the authenticator app.
      responses:
        "204":
          description: Two-factor is now enforced at login.
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "409": { $ref: "#/components/responses/Conflict" }

  /v1/auth/2fa/disable:
    post:
      operationId: disableTwoFactor
      summary: Disable two-factor authentication
      description: |
        Removes a confirmed enrollment or cancels a pending one. Sudo-mode
        gated; identity proof comes from the reauth window, so there is no
        request body.
      tags: [auth]
      responses:
        "204":
          description: Two-factor disabled.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/ReauthRequired" }
        "409": { $ref: "#/components/responses/Conflict" }

  /v1/auth/2fa/backup-codes:
    post:
      operationId: regenerateBackupCodes
      summary: Regenerate backup codes
      description: |
        Replaces all existing backup codes, used and unused. Sudo-mode gated;
        no request body.
      tags: [auth]
      responses:
        "200":
          description: Fresh backup codes; the previous ones are dead.
          content:
            application/json:
              schema:
                type: object
                required: [backup_codes]
                properties:
                  backup_codes:
                    type: array
                    items: { type: string }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/ReauthRequired" }
        "409": { $ref: "#/components/responses/Conflict" }

  /v1/users:
    get:
      operationId: listUsers
      summary: List all users
      description: Admin only. Every user of the instance, oldest first.
      tags: [users]
      responses:
        "200":
          description: All users.
          content:
            application/json:
              schema:
                type: object
                required: [users]
                properties:
                  users:
                    type: array
                    items: { $ref: "#/components/schemas/User" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
    post:
      operationId: createUser
      summary: Create a user
      description: |
        Admin only. Provisions a user with an initial password; there is no
        self-service signup or invitation email by design. `role` defaults
        to `member`.
      tags: [users]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [email, password]
              properties:
                email:
                  type: string
                  format: email
                  maxLength: 254
                name:
                  type: string
                password:
                  type: string
                  minLength: 8
                role: { $ref: "#/components/schemas/Role" }
      responses:
        "201":
          description: User created.
          content:
            application/json:
              schema:
                type: object
                required: [user]
                properties:
                  user: { $ref: "#/components/schemas/User" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /v1/users/{id}:
    patch:
      operationId: updateUser
      summary: Update a user's name or role
      description: |
        Admin only. Changing your own role is refused (`409`), as is
        demoting the last admin.
      tags: [users]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: At least one property must be present.
              properties:
                name:
                  type: string
                role: { $ref: "#/components/schemas/Role" }
      responses:
        "200":
          description: The updated user.
          content:
            application/json:
              schema:
                type: object
                required: [user]
                properties:
                  user: { $ref: "#/components/schemas/User" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    delete:
      operationId: deleteUser
      summary: Delete a user
      description: |
        Admin only. Deletes the user with their sessions and 2FA enrollment.
        Deleting yourself or the last admin is refused (`409`).
      tags: [users]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: User deleted.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /v1/users/{id}/password:
    post:
      operationId: resetUserPassword
      summary: Reset a user's password
      description: |
        Admin only. Sets a new password without the old one — the recovery
        path, since there is no email-based reset. Revokes every session of
        the user.
      tags: [users]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              required: [new_password]
              properties:
                new_password:
                  type: string
                  minLength: 8
      responses:
        "204":
          description: Password reset; all of the user's sessions are revoked.
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /v1/nodes:
    get:
      operationId: listNodes
      summary: List cluster nodes
      description: |
        Admin only. Every node of the cluster, oldest first — including the
        master's own row. `status` is derived from heartbeats (a node flips
        `offline` after missing them for the staleness window).
      tags: [nodes]
      responses:
        "200":
          description: All nodes.
          content:
            application/json:
              schema:
                type: object
                required: [nodes]
                properties:
                  nodes:
                    type: array
                    items: { $ref: "#/components/schemas/Node" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }

  /v1/nodes/tokens:
    post:
      operationId: createJoinToken
      summary: Mint a one-time node join token
      description: |
        Admin only, sudo mode. Creates a single-use enrollment token (valid
        for one hour) and the ready-to-paste `skalid enroll` command for the
        new machine. The token embeds a fingerprint of the cluster CA, so
        the enrolling node can authenticate the master before trusting it.
        Requires `CLUSTER_ADDR` to be configured on the master (`409`).
        The token is shown exactly once.
      tags: [nodes]
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              properties:
                roles:
                  type: array
                  description: Roles granted at enrollment. Defaults to `[worker]`.
                  items:
                    type: string
                    enum: [edge, worker, builder]
      responses:
        "201":
          description: Join token created.
          content:
            application/json:
              schema: { $ref: "#/components/schemas/JoinTokenCreated" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "409": { $ref: "#/components/responses/Conflict" }

  /v1/nodes/{id}/metrics:
    get:
      operationId: listNodeMetrics
      summary: Node metrics history
      description: |
        Admin only. The node's resource history over the last 24 hours,
        averaged into 2-minute buckets (at most 720 samples). Buckets in which
        the node reported nothing are absent — offline windows appear as gaps,
        never as zeros.
      tags: [nodes]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Bucketed metrics history, oldest first.
          content:
            application/json:
              schema:
                type: object
                required: [samples]
                properties:
                  samples:
                    type: array
                    items: { $ref: "#/components/schemas/NodeMetricsSample" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }

  /v1/nodes/{id}:
    patch:
      operationId: updateNode
      summary: Update a node's name, roles, or public address
      description: |
        Admin only, sudo mode. The `master` role is fixed — it can be
        neither granted nor removed (`409`). Setting `public_addr` to an
        empty string clears it.
      tags: [nodes]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema:
              type: object
              description: At least one property must be present.
              properties:
                name:
                  type: string
                roles:
                  type: array
                  items: { $ref: "#/components/schemas/NodeRole" }
                public_addr:
                  type: string
                  description: Where public DNS points for this node (edge role). Empty string clears it.
      responses:
        "200":
          description: The updated node.
          content:
            application/json:
              schema:
                type: object
                required: [node]
                properties:
                  node: { $ref: "#/components/schemas/Node" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
    delete:
      operationId: deleteNode
      summary: Remove a node from the cluster
      description: |
        Admin only, sudo mode. Deletes the node row and implicitly revokes
        the node's certificate (its serial no longer matches any node). The
        machine keeps running but is orphaned; re-adding it requires a fresh
        join token. The master node cannot be removed (`409`).
      tags: [nodes]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "204":
          description: Node removed.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }

  /v1/nodes/{id}/containers:
    get:
      operationId: listNodeContainers
      summary: List a node's containers
      description: |
        Admin only. The node's skali-managed containers as last observed by
        the master (heartbeat cadence, ~15s; mutations through this API
        reflect immediately). Containers that disappeared from the node keep
        a `gone` row for up to an hour, then vanish.

        This whole surface is a low-level admin escape hatch; the typed
        resource APIs (applications, databases) are the product surface.
      tags: [containers]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      responses:
        "200":
          description: Observed containers, sorted by name.
          content:
            application/json:
              schema:
                type: object
                required: [containers]
                properties:
                  containers:
                    type: array
                    items: { $ref: "#/components/schemas/Container" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
    post:
      operationId: createNodeContainer
      summary: Create a container on a node
      description: |
        Admin only, sudo mode. Creates (and by default starts) a container
        from a raw spec on the given node. The container is stamped
        `skali.managed=true` plus the given `kind`; user labels must not use
        the `skali.` prefix. Pulling a missing image can take minutes — the
        request blocks until the container exists (up to a 5-minute
        server-side ceiling).
      tags: [containers]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
      requestBody:
        required: true
        content:
          application/json:
            schema: { $ref: "#/components/schemas/ContainerCreateRequest" }
      responses:
        "201":
          description: The created container as observed post-start.
          content:
            application/json:
              schema:
                type: object
                required: [container]
                properties:
                  container: { $ref: "#/components/schemas/Container" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "502": { $ref: "#/components/responses/NodeUnreachable" }

  /v1/nodes/{id}/containers/{cid}/start:
    post:
      operationId: startNodeContainer
      summary: Start a container
      description: Admin only, sudo mode.
      tags: [containers]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: cid
          in: path
          required: true
          schema:
            type: string
          description: Engine container id.
      responses:
        "200":
          description: The container's post-start state.
          content:
            application/json:
              schema:
                type: object
                required: [container]
                properties:
                  container: { $ref: "#/components/schemas/Container" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "502": { $ref: "#/components/responses/NodeUnreachable" }

  /v1/nodes/{id}/containers/{cid}/stop:
    post:
      operationId: stopNodeContainer
      summary: Stop a container
      description: |
        Admin only, sudo mode. Graceful stop; the optional body sets how long
        the engine waits before killing (engine default when omitted).
      tags: [containers]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: cid
          in: path
          required: true
          schema:
            type: string
          description: Engine container id.
      requestBody:
        required: false
        content:
          application/json:
            schema:
              type: object
              properties:
                timeout_seconds:
                  type: integer
                  minimum: 0
      responses:
        "200":
          description: The container's post-stop state.
          content:
            application/json:
              schema:
                type: object
                required: [container]
                properties:
                  container: { $ref: "#/components/schemas/Container" }
        "400": { $ref: "#/components/responses/BadRequest" }
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "502": { $ref: "#/components/responses/NodeUnreachable" }

  /v1/nodes/{id}/containers/{cid}:
    delete:
      operationId: deleteNodeContainer
      summary: Remove a container
      description: |
        Admin only, sudo mode. Removing a running container requires
        `force=true` (`409` otherwise). Idempotent: a container the node no
        longer knows still deletes the master's record and returns `204`.
      tags: [containers]
      parameters:
        - name: id
          in: path
          required: true
          schema:
            type: string
            format: uuid
        - name: cid
          in: path
          required: true
          schema:
            type: string
          description: Engine container id.
        - name: force
          in: query
          required: false
          schema:
            type: boolean
            default: false
      responses:
        "204":
          description: Container removed.
        "401": { $ref: "#/components/responses/Unauthorized" }
        "403": { $ref: "#/components/responses/Forbidden" }
        "404": { $ref: "#/components/responses/NotFound" }
        "409": { $ref: "#/components/responses/Conflict" }
        "502": { $ref: "#/components/responses/NodeUnreachable" }

components:
  securitySchemes:
    bearerAuth:
      type: http
      scheme: bearer
      description: Opaque session token from login / 2FA verify.

  responses:
    BadRequest:
      description: Malformed request body or failed validation.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    Unauthorized:
      description: Missing/invalid token, wrong credentials, or wrong code.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    Forbidden:
      description: |
        The caller is authenticated but lacks the admin role
        (`error.code = forbidden`), or — on sudo-mode-gated writes — recent
        authentication is required (`error.code = reauth_required`).
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    NotFound:
      description: The resource does not exist or belongs to another user.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    Conflict:
      description: The resource is in a state that does not allow the operation.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    RateLimited:
      description: Too many attempts; retry after the window passes.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    ReauthRequired:
      description: |
        Recent authentication required (`error.code = reauth_required`):
        the session's sudo-mode window has lapsed. Confirm identity at
        `/v1/auth/reauth` and retry the original request.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }
    NodeUnreachable:
      description: |
        The node (or its container engine) could not be reached
        (`error.code = node_unreachable`). The cluster state is unchanged;
        retry once the node is back.
      content:
        application/json:
          schema: { $ref: "#/components/schemas/Error" }

  schemas:
    Role:
      type: string
      description: |
        Instance-wide role. Admins additionally manage users and instance
        settings; members have full access to all projects.
      enum: [admin, member]

    User:
      type: object
      required: [id, email, name, role, two_factor_enabled, created_at]
      properties:
        id:
          type: string
          format: uuid
        email:
          type: string
          format: email
        name:
          type: string
        role: { $ref: "#/components/schemas/Role" }
        two_factor_enabled:
          type: boolean
        created_at:
          type: string
          format: date-time

    NodeRole:
      type: string
      description: |
        A node's responsibilities. `master` runs the control plane (one per
        cluster, assigned at boot, immutable); `edge` terminates public
        ingress; `worker` runs application containers; `builder` builds
        images.
      enum: [master, edge, worker, builder]

    Node:
      type: object
      required: [id, name, roles, advertise_addr, status, created_at, updated_at]
      properties:
        id:
          type: string
          format: uuid
        name:
          type: string
        roles:
          type: array
          items: { $ref: "#/components/schemas/NodeRole" }
        advertise_addr:
          type: string
          description: host:port the master dials on the private network.
        public_addr:
          type: [string, "null"]
          description: Where public DNS points (edge role); operator-set.
        arch:
          type: [string, "null"]
          description: Reported by heartbeat (e.g. amd64, arm64).
        os:
          type: [string, "null"]
        skalid_version:
          type: [string, "null"]
        status:
          type: string
          enum: [online, offline]
        last_seen:
          type: [string, "null"]
          format: date-time
        metrics:
          oneOf:
            - { $ref: "#/components/schemas/NodeMetrics" }
            - type: "null"
          description: Latest resource snapshot; null until the node first reports.
        created_at:
          type: string
          format: date-time
        updated_at:
          type: string
          format: date-time

    NodeMetrics:
      type: object
      description: |
        A node resource snapshot. Rates are bytes/second; `cpu_pct` is 0–100
        normalized across effective cores (the cgroup quota when the node runs
        in a limited container); `load1` is the host-wide 1-minute load average.
      required:
        [cpu_pct, mem_used, mem_total, disk_used, disk_total,
         net_rx_rate, net_tx_rate, disk_read_rate, disk_write_rate, load1]
      properties:
        cpu_pct: { type: number }
        mem_used: { type: integer }
        mem_total: { type: integer }
        disk_used: { type: integer }
        disk_total: { type: integer }
        net_rx_rate: { type: integer }
        net_tx_rate: { type: integer }
        disk_read_rate: { type: integer }
        disk_write_rate: { type: integer }
        load1: { type: number }

    NodeMetricsSample:
      allOf:
        - { $ref: "#/components/schemas/NodeMetrics" }
        - type: object
          required: [sampled_at]
          properties:
            sampled_at:
              type: string
              format: date-time
              description: Bucket timestamp (~8-minute averages).

    ContainerKind:
      type: string
      description: |
        How skali manages a container. `application` instances belong to a
        project; a `database` container is an engine pool that may host
        logical databases of many projects; `system` containers are skali's
        own infrastructure (proxy, registry, ...).
      enum: [application, database, system]

    Container:
      type: object
      description: One observed skali-managed container on a node.
      required:
        [id, name, image, kind, state, health, exit_code, restart_count,
         labels, stats, created_at, started_at, first_seen, last_seen]
      properties:
        id:
          type: string
          description: Engine container id.
        name:
          type: string
        image:
          type: string
        kind: { $ref: "#/components/schemas/ContainerKind" }
        state:
          type: string
          description: |
            Engine-reported state, plus `gone` for containers that vanished
            from the node's reports (kept briefly for visibility).
          enum: [created, running, paused, restarting, removing, exited, dead, gone]
        health:
          type: [string, "null"]
          description: Healthcheck state; null when none is configured.
          enum: [starting, healthy, unhealthy, null]
        exit_code:
          type: [integer, "null"]
        restart_count:
          type: integer
        labels:
          type: object
          additionalProperties: { type: string }
          description: Full label map including the skali.* identity labels.
        stats:
          oneOf:
            - { $ref: "#/components/schemas/ContainerStats" }
            - type: "null"
          description: Latest stats; null until the node has sampled twice.
        created_at:
          type: [string, "null"]
          format: date-time
          description: The container's own creation time on the engine.
        started_at:
          type: [string, "null"]
          format: date-time
        first_seen:
          type: string
          format: date-time
        last_seen:
          type: string
          format: date-time

    ContainerStats:
      type: object
      description: |
        Per-container resource snapshot. `cpu_pct` uses docker-stats
        semantics — 100 means one full core, so values can exceed 100 on
        multi-core containers. Rates are bytes/second.
      required: [cpu_pct, mem_used, mem_limit, net_rx_rate, net_tx_rate]
      properties:
        cpu_pct: { type: number }
        mem_used: { type: integer }
        mem_limit:
          type: integer
          description: As reported by the engine; the host total when unlimited.
        net_rx_rate: { type: integer }
        net_tx_rate: { type: integer }

    ContainerCreateRequest:
      type: object
      required: [name, image, kind]
      properties:
        name:
          type: string
          pattern: "^[a-zA-Z0-9][a-zA-Z0-9_.-]{0,127}$"
        image:
          type: string
        kind: { $ref: "#/components/schemas/ContainerKind" }
        env:
          type: object
          additionalProperties: { type: string }
        command:
          type: array
          items: { type: string }
          description: Overrides the image CMD.
        mounts:
          type: array
          items:
            type: object
            required: [type, source, target]
            properties:
              type:
                type: string
                enum: [bind, volume]
              source:
                type: string
                description: Host path (bind) or volume name (volume).
              target:
                type: string
              read_only:
                type: boolean
                default: false
        ports:
          type: array
          items:
            type: object
            required: [container_port]
            properties:
              host_ip:
                type: string
                description: Empty binds all interfaces.
              host_port:
                type: integer
                description: 0 picks an ephemeral port.
              container_port:
                type: integer
              protocol:
                type: string
                enum: [tcp, udp]
                default: tcp
        restart_policy:
          type: string
          enum: [no, always, unless-stopped, on-failure]
          default: no
        restart_max_retries:
          type: integer
          description: Only valid with `on-failure`.
        cpus:
          type: number
          description: CPU limit in cores (0.5 = half a core); 0 = unlimited.
        memory_limit:
          type: integer
          description: Memory limit in bytes; 0 = unlimited.
        networks:
          type: array
          items: { type: string }
          description: Existing engine network names to attach.
        labels:
          type: object
          additionalProperties: { type: string }
          description: User labels; the `skali.` prefix is reserved (400).
        healthcheck:
          type: object
          required: [test]
          properties:
            test:
              type: array
              items: { type: string }
              description: E.g. ["CMD-SHELL", "curl -f http://localhost/ || exit 1"].
            interval_seconds: { type: integer }
            timeout_seconds: { type: integer }
            start_period_seconds: { type: integer }
            retries: { type: integer }
        pull:
          type: string
          enum: [if-missing, always, never]
          default: if-missing
        start:
          type: boolean
          default: true
          description: Start the container after creating it.

    JoinTokenCreated:
      type: object
      description: A freshly minted join token. Shown exactly once.
      required: [token, expires_at, enroll_command]
      properties:
        token:
          type: string
          description: One-time enrollment secret, `<id>.<secret>.<ca-fingerprint>`.
        expires_at:
          type: string
          format: date-time
        enroll_command:
          type: string
          description: Ready-to-paste command for the new machine.

    Session:
      type: object
      description: A session as listed in session management; never carries the token.
      required: [id, expires_at, ip_address, user_agent, created_at, current]
      properties:
        id:
          type: string
          format: uuid
        expires_at:
          type: string
          format: date-time
        ip_address:
          type: string
        user_agent:
          type: string
        created_at:
          type: string
          format: date-time
        current:
          type: boolean
          description: True for the session the request authenticated with.

    SessionCreated:
      type: object
      description: A freshly created session. The token is shown exactly once.
      required: [token, expires_at, user]
      properties:
        token:
          type: string
          description: Opaque bearer token; store it securely.
        expires_at:
          type: string
          format: date-time
        user: { $ref: "#/components/schemas/User" }

    SessionCreatedResponse:
      type: object
      required: [session]
      properties:
        session: { $ref: "#/components/schemas/SessionCreated" }

    Challenge:
      type: object
      required: [token, expires_at]
      properties:
        token:
          type: string
          description: Single-use challenge token for /v1/auth/2fa/verify.
        expires_at:
          type: string
          format: date-time

    ChallengeResponse:
      type: object
      required: [challenge]
      properties:
        challenge: { $ref: "#/components/schemas/Challenge" }

    TwoFactorEnrollment:
      type: object
      required: [secret, otpauth_uri, backup_codes]
      properties:
        secret:
          type: string
          description: Base32 TOTP secret, for manual authenticator entry.
        otpauth_uri:
          type: string
          description: otpauth:// URI to render as a QR code.
        backup_codes:
          type: array
          items: { type: string }
          description: Single-use recovery codes; shown only once.

    Error:
      type: object
      required: [error]
      properties:
        error:
          type: object
          required: [code, message]
          properties:
            code:
              type: string
              enum:
                - bad_request
                - invalid_credentials
                - invalid_token
                - invalid_code
                - forbidden
                - reauth_required
                - not_found
                - conflict
                - rate_limited
                - node_unreachable
                - internal
            message:
              type: string
