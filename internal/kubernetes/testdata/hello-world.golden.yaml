apiVersion: traefik.io/v1alpha1
kind: Middleware
metadata:
  labels:
    skali.dev/managed: "true"
    skali.dev/project: hello-world
  name: redirect-https
  namespace: skali-hello-world
spec:
  redirectScheme:
    permanent: true
    scheme: https
---
apiVersion: apps/v1
kind: Deployment
metadata:
  labels:
    app.kubernetes.io/name: hello-world-web
    skali.dev/application: web
    skali.dev/managed: "true"
    skali.dev/project: hello-world
    skali.dev/service: web
  name: hello-world-web
  namespace: skali-hello-world
spec:
  replicas: 3
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: hello-world-web
      skali.dev/application: web
      skali.dev/managed: "true"
      skali.dev/project: hello-world
  strategy:
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
    type: RollingUpdate
  template:
    metadata:
      labels:
        app.kubernetes.io/name: hello-world-web
        skali.dev/application: web
        skali.dev/managed: "true"
        skali.dev/project: hello-world
        skali.dev/service: web
    spec:
      containers:
      - image: localhost:5510/skali/hello-world/web@sha256:1111111111111111111111111111111111111111111111111111111111111111
        imagePullPolicy: IfNotPresent
        livenessProbe:
          failureThreshold: 3
          httpGet:
            path: /healthz
            port: http
          periodSeconds: 30
          timeoutSeconds: 2
        name: web
        ports:
        - containerPort: 8080
          name: http
          protocol: TCP
        readinessProbe:
          failureThreshold: 3
          httpGet:
            path: /healthz
            port: http
          periodSeconds: 5
          timeoutSeconds: 2
        resources: {}
      priorityClassName: skali-normal
      terminationGracePeriodSeconds: 30
      topologySpreadConstraints:
      - labelSelector:
          matchLabels:
            app.kubernetes.io/name: hello-world-web
            skali.dev/application: web
            skali.dev/managed: "true"
            skali.dev/project: hello-world
        maxSkew: 1
        topologyKey: kubernetes.io/hostname
        whenUnsatisfiable: ScheduleAnyway
---
apiVersion: v1
kind: Service
metadata:
  labels:
    app.kubernetes.io/name: hello-world-web
    skali.dev/application: web
    skali.dev/managed: "true"
    skali.dev/project: hello-world
    skali.dev/service: web
  name: hello-world-web
  namespace: skali-hello-world
spec:
  ports:
  - appProtocol: http
    name: http
    port: 8080
    protocol: TCP
    targetPort: http
  selector:
    app.kubernetes.io/name: hello-world-web
    skali.dev/application: web
    skali.dev/managed: "true"
    skali.dev/project: hello-world
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  labels:
    app.kubernetes.io/name: hello-world-web
    skali.dev/application: web
    skali.dev/managed: "true"
    skali.dev/project: hello-world
    skali.dev/service: web
  name: hello-world-web-public
  namespace: skali-hello-world
spec:
  entryPoints:
  - websecure
  routes:
  - kind: Rule
    match: Host(`hello.localhost`) && PathPrefix(`/`)
    services:
    - name: hello-world-web
      port: http
  tls:
    secretName: hello-world-web-public-tls
---
apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  labels:
    app.kubernetes.io/name: hello-world-web
    skali.dev/application: web
    skali.dev/managed: "true"
    skali.dev/project: hello-world
    skali.dev/service: web
  name: hello-world-web-public-http
  namespace: skali-hello-world
spec:
  entryPoints:
  - web
  routes:
  - kind: Rule
    match: Host(`hello.localhost`) && PathPrefix(`/`)
    middlewares:
    - name: redirect-https
    services:
    - name: hello-world-web
      port: http
---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  labels:
    app.kubernetes.io/name: hello-world-web
    skali.dev/application: web
    skali.dev/managed: "true"
    skali.dev/project: hello-world
    skali.dev/service: web
  name: hello-world-web-public-tls
  namespace: skali-hello-world
spec:
  dnsNames:
  - hello.localhost
  issuerRef:
    group: cert-manager.io
    kind: ClusterIssuer
    name: skali
  secretName: hello-world-web-public-tls
